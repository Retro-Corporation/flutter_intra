/// Rendering variants for [ExerciseCardSkeleton].
enum ExerciseCardSkeletonVariant {
  /// Full skeleton — matches [ExerciseCardReadVariant.full] dimensions.
  full,

  /// Simple skeleton — matches [ExerciseCardReadVariant.simple] dimensions.
  simple,
}

/// Data and layout types for [LabelOptionPickerPanel].
class LabelOption {
  final String id;
  final String label;
  const LabelOption({required this.id, required this.label});
}

enum LabelPickerLayout { column, grid2 }

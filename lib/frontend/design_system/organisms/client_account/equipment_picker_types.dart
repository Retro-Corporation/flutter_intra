/// A single selectable equipment option shown in [EquipmentPickerPanel].
class EquipmentOption {
  final String id;
  final String label;

  const EquipmentOption({required this.id, required this.label});
}

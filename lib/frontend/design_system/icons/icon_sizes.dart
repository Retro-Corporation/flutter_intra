/// Design tokens: Icon sizes.
/// Derived from the 8-point grid via rem scaling (root = 16.0).
class IconSizes {
  IconSizes._();

  /// 0.5rem → 8px
  static const double sm = 8.0;

  /// 1rem → 16px
  static const double md = 16.0;

  /// 1.5rem → 24px
  static const double lg = 24.0;

  /// 2rem → 32px
  static const double xl = 32.0;
}

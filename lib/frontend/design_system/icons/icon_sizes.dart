import '../foundation/grid.dart';

/// Design tokens: Icon sizes.
/// Derived from the 8-point grid via rem scaling.
class IconSizes {
  IconSizes._();

  /// 0.5rem → 8px
  static final double sm = 0.5.rem;

  /// 1rem → 16px
  static final double md = 1.rem;

  /// 1.5rem → 24px
  static final double lg = 1.5.rem;

  /// 2rem → 32px
  static final double xl = 2.rem;
}
